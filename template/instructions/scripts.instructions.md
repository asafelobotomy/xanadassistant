---
name: Scripts
applyTo: "scripts/**"
description: "Conventions for scripts in this workspace — language, style, and dependency discipline"
---

# Script Instructions

- Primary language for this workspace: **{{PRIMARY_LANGUAGE}}**
- Package manager: **{{PACKAGE_MANAGER}}**
- Keep scripts small and focused — one responsibility per file.
- No silent error swallowing: fail loudly with a non-zero exit code or a structured error.
- Prefer stdlib or built-in tooling over introducing new dependencies.
- Scripts that are executable must have a proper shebang line and be marked executable in the repo.
- Do not hard-code paths that vary by environment — use environment variables or config.
- Generated or derived artifacts should be regenerated by a named script, not edited by hand.
