# Contract Examples

This directory holds machine-facing example payloads and event streams for the lifecycle contracts.

Use these files as reference fixtures when reviewing contract changes, CLI output shape, or regression coverage. Keep them aligned with the corresponding contract documents and any tests that assert serialized payload structure.
