name: "docsReview-eval"
description: "Evaluates docsReview skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(accuracy:|missing:|clarity:)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
