id: basic-invocation
description: "Verify skill produces output appropriate for lean output formatting"
prompt: |
  Run the leanOutput skill. Apply it to a relevant example and produce the
  expected output, checklist findings, or recommendations as defined by the skill.
expected:
  - "added:"
tags:
  - basic
  - smoke
