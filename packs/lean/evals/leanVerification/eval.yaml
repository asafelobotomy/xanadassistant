name: "leanVerification-eval"
description: "Evaluates leanVerification skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(Ran:|pass|fail)"

tasks:
  - "tasks/*.yaml"
