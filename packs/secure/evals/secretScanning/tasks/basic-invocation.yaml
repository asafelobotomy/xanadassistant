id: basic-invocation
description: "Verify skill produces output appropriate for secret scanning before commit"
prompt: |
  Run the secretScanning skill. Apply it to a relevant example and produce the
  expected output, checklist findings, or recommendations as defined by the skill.
expected:
  - "secret"
tags:
  - basic
  - smoke
