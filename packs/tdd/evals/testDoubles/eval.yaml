name: "testDoubles-eval"
description: "Evaluates testDoubles skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(stub|mock|fake|spy|dummy)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
