name: "testCoverage-eval"
description: "Evaluates testCoverage skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(branch coverage|mutation|diagnostic)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
