id: basic-invocation
description: "Verify skill produces output appropriate for ML model serving deployment"
prompt: |
  Run the mlopsModelServing skill. Apply it to a relevant example and produce the
  expected output, checklist findings, or recommendations as defined by the skill.
expected:
  - "registry"
tags:
  - basic
  - smoke
