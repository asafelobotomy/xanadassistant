name: "mlopsExperiments-eval"
description: "Evaluates mlopsExperiments skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(reproducib|seed|baseline|metric)"

tasks:
  - "tasks/*.yaml"
