id: basic-invocation
description: "Verify skill produces output appropriate for container image or Dockerfile review"
prompt: |
  Run the devopsContainers skill. Apply it to a relevant example and produce the
  expected output, checklist findings, or recommendations as defined by the skill.
expected:
  - "Dockerfile or container"
tags:
  - basic
  - smoke
