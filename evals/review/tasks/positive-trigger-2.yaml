id: positive-trigger-2
description: "Verify review agent prefers structured filesystem MCP inspection before generic shell fallback"
prompt: |
  Which agent should stay read-only while reviewing a repo-wide diff, and if the filesystem server is connected should it prefer read_file, list_directory, search_files, and file_info before falling back to runCommands?
expected:
  - "review"
  - "filesystem"
  - "read_file"
  - "runCommands"
tags:
  - basic
  - smoke
  - positive