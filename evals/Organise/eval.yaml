name: "Organise-eval"
description: "Evaluates Organise agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Organise|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
