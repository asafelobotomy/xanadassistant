name: "Deps-eval"
description: "Evaluates Deps agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Deps|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
