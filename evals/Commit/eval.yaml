name: "Commit-eval"
description: "Evaluates Commit agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Commit|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
