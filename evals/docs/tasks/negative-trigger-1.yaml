id: negative-trigger-1
description: "Verify agent does NOT trigger on an unrelated request"
prompt: |
  What is the current time and date?
expected_absent:
  - "(?i)\\bdocs\\b"
tags:
  - smoke
  - negative
