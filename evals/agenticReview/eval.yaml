name: "agenticReview-eval"
description: "Evaluates agenticReview skill behaviour"

graders:
  - type: text
    name: references_skill
    config:
      pattern: "(?i)(agenticReview|skill)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
