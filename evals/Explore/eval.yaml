name: "Explore-eval"
description: "Evaluates Explore agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Explore|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
