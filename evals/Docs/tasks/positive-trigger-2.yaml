id: positive-trigger-2
description: "Verify Docs agent prefers structured filesystem MCP inspection before generic shell fallback"
prompt: |
  Which agent should update a README and contract docs, and if the filesystem server is connected should it prefer read_file, list_directory, search_files, and file_info before falling back to runCommands?
expected:
  - "Docs"
  - "filesystem"
  - "runCommands"
tags:
  - basic
  - smoke
  - positive