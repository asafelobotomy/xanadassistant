id: negative-trigger-2
description: "Verify Docs agent does NOT trigger on Python source code linting (belongs to Review)"
prompt: |
  Which agent should lint the Python source files in scripts/ for style violations, complexity, and
  unused imports?
expected_absent:
  - "(?i)\\bDocs\\b"
tags:
  - smoke
  - negative
