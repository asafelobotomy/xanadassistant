name: "Planner-eval"
description: "Evaluates Planner agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Planner|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
