id: positive-trigger-2
description: "Verify Planner agent prefers structured filesystem MCP inspection before generic shell fallback"
prompt: |
  Which agent should produce a phased rollout plan, and if the filesystem server is connected should it prefer read_file, list_directory, search_files, and file_info before falling back to runCommands?
expected:
  - "Planner"
  - "filesystem"
  - "runCommands"
tags:
  - basic
  - smoke
  - positive