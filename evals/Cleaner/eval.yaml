name: "Cleaner-eval"
description: "Evaluates Cleaner agent routing behaviour and response bounds"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?is)(\\bCleaner\\b.*repository hygiene|repository hygiene.*\\bCleaner\\b)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
