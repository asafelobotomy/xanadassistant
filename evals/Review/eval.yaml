name: "Review-eval"
description: "Evaluates Review agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Review|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
