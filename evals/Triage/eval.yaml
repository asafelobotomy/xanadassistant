name: "Triage-eval"
description: "Evaluates Triage agent behaviour"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(Triage|agent)"

  - type: behavior
    name: completion_bound
    config:
      max_tokens: 2000

tasks:
  - "tasks/*.yaml"
