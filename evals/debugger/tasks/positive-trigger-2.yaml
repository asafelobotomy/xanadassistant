id: positive-trigger-2
description: "Verify debugger agent prefers structured filesystem MCP inspection before generic shell fallback"
prompt: |
  Which agent should diagnose a failing test before implementation, and if the filesystem server is connected should it prefer read_file, list_directory, search_files, and file_info before falling back to runCommands?
expected:
  - "debugger"
  - "filesystem"
  - "read_file"
  - "runCommands"
tags:
  - basic
  - smoke
  - positive